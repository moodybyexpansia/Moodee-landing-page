---
import { List as DefaultList } from "astro-portabletext/components";
import type { Props as $, List } from "astro-portabletext/types";

export type Props = $<List>;

const props = Astro.props;
---

<DefaultList {...props}><slot /></DefaultList>

<style>
  ol {
    list-style: decimal;
    padding-left: 2rem;
  }
  ul {
    list-style: disc;
    padding-left: 2rem;
  }
</style>
